import * as AT from '../consts/ActionTypes'

const HelloWorldRedu = (state = {}, action) => {
    switch (action.type) {
        // case AT.LOADING:
        //     newState = { loading: true };
        //     return newState;
        default:
            return state;
    }
}

export default HelloWorldRedu